import {css} from 'styled-components';
import {getColor} from 'styles/helper';

export const InputLabel = css`
    font-size: 14px;
    color: ${getColor('shade', 'light')};
    padding-bottom: 2px;
`;
